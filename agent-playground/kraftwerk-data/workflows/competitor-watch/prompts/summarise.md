Read changes.md. For every page marked "changed", write two to four bullets
on what changed and why it could matter; for pages marked "first seen" write
one line saying the baseline was stored; skip pages marked "unchanged".
Write watch.md: first line "# Competitor watch" plus the date from
changes.md, then one "## <URL>" section per changed or first-seen page, and
a final "## Fazit" with one or two sentences. If nothing changed at all,
watch.md is the title, the line "Keine Änderungen seit dem letzten Lauf."
and nothing else. Use only what changes.md contains.
