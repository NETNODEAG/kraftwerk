{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/NETNODEAG/kraftwerk/main/kraftwerk/schema/kraftwerk.schema.json",
  "title": "kraftwerk project config",
  "description": "kraftwerk.yml at the project root: config file and root marker for the kraftwerk CLI. All fields are optional — an empty file is valid.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "description": "Display name of the project (\"environment\"), shown in the inspector header and browser tab"
    },
    "icon": {
      "type": "string",
      "description": "Emoji used as the inspector favicon (browser-tab icon), e.g. \"⚡\""
    },
    "port": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Port `kraftwerk ui` listens on. Default: 1981 (the --port flag wins over this)"
    },
    "workflows": {
      "type": "string",
      "description": "Workflows root relative to this file. Default: src/workflows or workflows"
    },
    "output": {
      "type": "string",
      "description": "Run-artifact directory relative to this file. Default: output"
    },
    "knowledge": {
      "type": "string",
      "description": "OKF knowledge-bundle root relative to this file. Default: knowledge"
    },
    "agents": {
      "type": "string",
      "description": "Team agent-definition root relative to this file. Default: agents"
    },
    "skills": {
      "type": "string",
      "description": "Workspace skill root (one folder per skill with a SKILL.md) relative to this file. Default: skills"
    },
    "switcher": {
      "type": "array",
      "description": "Other kraftwerk workspaces, shown as a switcher dropdown on the workspace name in the inspector header",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "url"
        ],
        "properties": {
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Display name of the other workspace"
          },
          "url": {
            "type": "string",
            "pattern": "^https?://",
            "description": "URL the other workspace's inspector runs on, e.g. \"https://localhost:1985\""
          },
          "icon": {
            "type": "string",
            "description": "Optional emoji shown next to the entry, e.g. \"🛰\""
          }
        }
      }
    },
    "git": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "description": "Git sync for the workspace: commit and push knowledge, agents, skills and workflows from the inspector. Absent = feature off, a bare `git:` = on with defaults.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Turn the feature off while keeping the config. Default: true when this block exists"
        },
        "remote": {
          "type": "string",
          "description": "Remote to fetch, pull and push. Default: origin"
        },
        "branch": {
          "type": "string",
          "description": "Branch to sync. Default: the checked-out branch"
        },
        "interval": {
          "type": "integer",
          "minimum": 0,
          "description": "Seconds between background fetches (and auto-pulls). 0 disables the timer. Default: 300"
        },
        "autosync": {
          "enum": [
            "off",
            "pull"
          ],
          "description": "off = fetch only. pull = also fast-forward when behind and the tree is clean. Default: pull"
        }
      }
    },
    "repos": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "description": "Repositories: git clones the agents work on, kept under one root inside the project. Absent = feature off, a bare `repos:` = on with defaults.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Turn the feature off while keeping the config. Default: true when this block exists"
        },
        "root": {
          "type": "string",
          "description": "Where clones land, relative to the project root. Should be git-ignored. Default: repos"
        }
      }
    }
  }
}
