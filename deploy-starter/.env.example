# --- agents -----------------------------------------------------------
# claude CLI + claude chat + pi:
ANTHROPIC_API_KEY=
# codex CLI + codex chat (alternative: docker compose exec ui codex login):
OPENAI_API_KEY=

# --- traefik override (compose.traefik.yml only) ----------------------
# KRAFTWERK_HOST=kraftwerk.example.com
# htpasswd -nB user — then double every $ ($ -> $$):
# KRAFTWERK_BASIC_AUTH=user:$$2y$$05$$...
